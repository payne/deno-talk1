reate demo.ts a portable application that will run on Mac, Windows, and Linux.  The application launches the default web
  browser to the URL http://localhost:8181.  Running there, from demo.ts, is a web application that has a plod form containing
  two text fields: summary & tags.  There's also a text area called description.  When the form is submitted it inserts into
  plod.db a sqlite database.  Under the form is a sortable table by summary and data column.  There's a filter field that works
  with summary, and tags.

